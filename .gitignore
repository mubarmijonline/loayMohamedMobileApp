.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.idea/
.vscode/
*.iml
*.ipr
*.iws
ios/Pods/
ios/Podfile.lock
macos/Pods/
macos/Podfile.lock
ios/Runner.xcworkspace/xcuserdata/
android/.gradle/
android/captures/
android/local.properties
android/app/build/
android/.idea/
android/app/release/
coverage/
.DS_Store

# Release signing secrets — never commit.
android/key.properties

# `build` is a symlink to a directory outside iCloud (see docs) — the trailing
# slash in `build/` does not match a symlink, so name it without one too.
/build
flutter_*.log

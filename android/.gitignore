# gradle-wrapper.jar and gradlew are committed on purpose, against the
# Flutter template default: without them a fresh clone has no pinned
# Gradle version and cannot build Android at all.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
